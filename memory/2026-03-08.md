## 2026-03-08 — Jess / Flatmates / MC debugging

- Post-reboot triage: NordVPN daemon installed/running but logged out; Tailscale actually up despite one misleading OpenClaw status readout.
- Jess failure after reboot was initially caused by proxy dependency timing: Jess used `socks5h://127.0.0.1:1080`, and startup happened before the SOCKS tunnel was ready.
- Confirmed `socks-tunnel.service` provides the local SOCKS proxy on `127.0.0.1:1080` via SSH dynamic port forwarding.
- Even after proxy recovery, Jess Flatmates inbox automation remained broken due to Flatmates anti-bot / protected login behaviour (`inputs=0`, login modal not rendered, likely 429/blocked path).
- Key architectural lesson: old Jess browser design (`chromium.launch` headless + fresh context + auto-login + cookie/session hacks) is not reliable for Flatmates and should be treated as unsuitable for inbox automation.
- Better target architecture chosen with owner: real persistent browser session, dedicated Brave/Chrome profile for Jess, and Jess attaching/controlling a real browser rather than repeatedly launching suspicious fresh automation browsers.
- Temporary code changes were made in `/home/diegopalhano/projects/jess-bot/jess-v2.js` to try persistent Brave profile mode (`/home/diegopalhano/projects/jess-bot/jess-profile`), but Flatmates still soft-blocked the automation path; a proper rebuild task was then handed off to Codex in the Jess repo.
- For Flatmates operations, practical rule communicated to owner: do not trust Jess inbox read/sync into Mission Control until the new real-browser architecture is completed.

## 2026-03-08 — Registration form / Mission Control

- Special test registration key `61416775321` exists in Mission Control registration keys and is mapped to `CO1` (`37 Marian St, Coorparoo`).
- Test flow intent: use `/r/61416775321` as a dummy/test registration path to validate the registration form and whether it communicates correctly with the Occupants area of Mission Control.
- User requested dummy prefill to default specifically to `CO1`, `Room 5`.
- `server.js` in Mission Control was updated so test key `61416775321` now pre-fills room as `Room 5` in the dummy data.
- Durable naming preference/update: user-facing Mission Control language was changed from `Tenants` to `Occupants` in visible UI labels (done in project, noted by user).
- `forms.housemates.online` currently fails with `DNS_PROBE_FINISHED_NXDOMAIN`; domain is not resolving, so that hostname is not usable until DNS is fixed.
- Separate from DNS, Mission Control also had runtime instability on port `8899`: `mission-control.service` repeatedly failed with `EADDRINUSE`, caused by stray/manual `node` processes already holding the port.
- When testing MC health, user successfully restarted `mission-control.service`, but port conflicts recurred later; future debugging should first check `lsof -i :8899 -P -n` / `ss -ltnp | grep 8899` before assuming route logic is broken.
- Practical fallback host to test `/r/:key` should be the working MC host (for example `mc.inspectionsxraytesting.com.au`) once port 8899 is stable, because `forms.housemates.online` is currently a DNS issue.

## 2026-03-08 — Evening session: Jess browser architecture + nginx + Kasada

### Kasada anti-bot — definitive findings
- **Headless Playwright (persistent profile)**: detected. Safe mode every time. Even with warmed profile (manual login → close → headless relaunch), Flatmates serves degraded DOM.
- **CDP attach to real Brave**: detected. Jess attached successfully (`attached-real-session` in logs) but Playwright's CDP navigation commands still trigger Kasada. Same degraded DOM result.
- **Mobile API (`api.flatmates.com.au`)**: exists, real Rails backend, routes `/messages`, `/login`, `/listings` respond. BUT also protected by Kasada (`x-kpsdk-*` headers). Direct API calls blocked without valid Kasada token.
- **Root cause confirmed**: Kasada detects Playwright itself, not just headless mode. Any programmatic navigation via Playwright CDP triggers bot detection.

### Jess code changes
- Default `BROWSER_CONNECT_MODE` changed from `'attach'` to `'persistent'` then back to `'attach'` in `/home/diegopalhano/projects/jess-bot/jess-v2.js` line 59 — currently set to `'attach'`.
- Xvfb (`xorg-x11-server-Xvfb`) not available in Rocky Linux 10 repos. TigerVNC also not available. Only Xwayland installed.
- Jess safe mode + Telegram alerting confirmed working correctly — new design is solid for health reporting.
- Boost monitor works in safe mode (checks listing edit pages).

### Proposed solution: Chrome extension approach
- Instead of Jess driving the browser (Playwright/CDP), build a Chrome extension that runs inside a normal Brave session.
- Extension reads Flatmates DOM (messages, conversations) and sends data to Jess/MC via localhost HTTP.
- Extension can also inject replies into Flatmates message box on Jess's command.
- **Kasada cannot detect this** — it's just normal JavaScript running in a page context, like any browser extension.
- Owner agreed this is the strongest realistic path. Awaiting confirmation to build.

### Nginx / domain migration
- Nginx config updated: `/etc/nginx/conf.d/housemates.conf` now has three separate server blocks:
  - `housemates.online` + `www.housemates.online` → returns blank `200` response
  - `mc.housemates.online` → proxy to `127.0.0.1:8899` (Mission Control) with WebSocket upgrade
  - `forms.housemates.online` → proxy to `127.0.0.1:8899` (registration forms)
- Nginx reloaded successfully.
- Cloudflare DNS: `forms` and `mc` A records added → `118.208.196.152` (Proxied). `www` CNAME exists. Root `@` A record was missing — user adding it.

### Atlas monitor issue
- `atlas-monitor.service` has DNS failure: `getaddrinfo ENOTFOUND api.telegram.org` — likely NordVPN/DNS resolution issue at boot. Needs restart or DNS fix.

### 1Password repo issue
- `dnf` operations are failing with GPG signature verification error for 1Password repo (`repomd.xml GPG signature verification error: Signing key not found`). Use `--disablerepo=1password` as workaround. Same for `tailscale-stable` repo intermittently.

## 2026-03-08 — Jess v3 created (7:43pm)

### Architecture
- **Jess v3** = relay-based, NO Playwright at all
- Chrome extension (`extension/`) scrapes Flatmates DOM from real Brave browser
- Relay server (`jess-relay.js`, port 3847) bridges extension ↔ Jess bot
- Jess v3 (`jess-v3.js`, 2256 lines) reads relay API, processes messages, Telegram approval flow
- Extension routes all relay calls through background script (mixed content fix)

### Files
- `jess-v3.js` — main bot
- `jess-v3.service` — systemd unit (depends on jess-relay.service)
- `jess-relay.js` — relay server
- `jess-relay.service` — systemd unit
- `extension/` — Chrome/Brave extension (manifest v3)
- `README-v3.md` — full setup + operation guide
- `JESS-V3-SPEC.md` — architecture spec given to Codex

### Git
- Commit `7a37d37` — `feat: Jess v3 — relay-based architecture, no Playwright`
- Backup at `/home/diegopalhano/backups/jess-v3-20260308-1949/`

### Status
- Relay running ✅, extension connected ✅, inbox data flowing ✅
- Jess v3 NOT yet activated (still on v2)
- Next: install services, switch to v3, test end-to-end

## 2026-03-08 — Evening session (20:00–20:34 AEST)

### Jess v3 — now fully live
- `jess-v3.service` + `jess-relay.service` both running, Jess v2 stopped/disabled
- Jess v3 confirmed processing inbox: Emily, Anthony, Samuel, Thomas, Joaquin, Leonardo, Mareva, Thiago, Brooke, others
- **Gemini 2.5 Flash** set as primary LLM for reply drafting (native Gemini API, `v1beta/models/gemini-2.5-flash:generateContent`)
  - Old OpenAI-compat endpoint was 404 — switched to native API
  - GOOGLE_API_KEY = GEMINI_API_KEY from `~/.bashrc`, added to `jess-v3.service`
  - Fallback chain: Gemini Flash → minimax-m2.5:cloud → kimi-k2.5:cloud → qwen3:8b → template
- Confirmed in logs: `Gemini Flash refined draft for Emily (49 chars)` ✅
- 7 replies queued for approval in first cycle, polling every 5 min
- Git commits: `ed6294d` (Google Flash added), `107fcd6` (native API fix)
- Desktop shortcut created: `/home/diegopalhano/Desktop/Open-Flatmates.sh`

### Monitor bot — new token
- Monitor bot moved to new bot: `@updatemonibot` (token: stored in monitor.js only)
- Old bot `@jessapprovals_bot` and `@Orbitv1_bot` left unchanged
- Monitor updated to track `jess-v3.service` (was jess-v2)
- `/ra` now restarts: whatsapp-bridge, jess-v3, mission-control, cloudflared, jess-relay
- Service needs restart to apply: `sudo systemctl restart atlas-monitor.service`

### Mission Control — crash loop root cause found
- MC user service has `ExecStartPre=/bin/bash -c "fuser -k 8899/tcp"` — kills everything on 8899 before starting
- Combined with `Restart=always RestartSec=5` → kills its own previous instance every 5s
- Fix: remove `ExecStartPre` line from service file
- `/tmp/mc-server.log` = MC stdout/stderr

### housemates.online — DNS still needed
- `mc.housemates.online` and `forms.housemates.online` still need DNS fix in Cloudflare:
  - Delete A records for `mc` and `forms`
  - Add CNAME → `445b0cfc-bb9d-4240-9bb8-769ad4b24c58.cfargotunnel.com` (Proxied)
- Currently still returning Rocky Linux default page (A record hits nginx default block)

### Outstanding tasks
1. Fix MC service file (remove fuser ExecStartPre)
2. Fix Cloudflare DNS for mc + forms subdomains (CNAME to tunnel)
3. Test `forms.housemates.online/r/61416775321`
4. Restart atlas-monitor with new token
5. Test end-to-end reply send (approve one in @jessapprovals_bot)

## 2026-03-08 Evening — Protocols, Team Routing, DNS, Jess improvements (21:00–21:54)

### PROTOCOLS.md created
- New file: `PROTOCOLS.md` in workspace root — loaded every session
- Contains: DIN rule, full team roster, agent routing table, hard rules, session checklist, key paths
- AGENTS.md updated: step 2 of every-session checklist now reads PROTOCOLS.md before USER.md
- Committed: `eb8238c`

### DIN Protocol clarified (owner instruction)
- Atlas delegates by default, always stays available to owner
- DIN = owner appends "DIN" → Atlas executes directly that task only, then returns to orchestrator
- Without DIN → always spawn agent, never implement inline
- PROTOCOLS.md + SOUL.md updated to reflect this
- Committed: `38eeaea`

### Cloudflare DNS + nginx fix
- Owner changed mc + forms DNS records from A to CNAME → `445b0cfc-bb9d-4240-9bb8-769ad4b24c58.cfargotunnel.com` (Proxied)
- Cloudflare flattens CNAME to A records — that is expected behaviour, not a bug
- Tunnel was routing correctly but nginx default `server_name _;` block was catching tunnel traffic (cloudflared connects via IPv6 `[::1]`)
- Fix: added `listen [::]:80;` to all server blocks in `/etc/nginx/conf.d/housemates.conf`
- Smith added duplicates (bug) → Atlas cleaned up manually (DIN) → nginx reload succeeded
- `mc.housemates.online` now live and serving Atlas Mission Control ✅

### Agent team — Smith confirmed as primary engineering agent
- Sessions spawned via `sessions_spawn(agentId: "smith", runtime: "subagent", mode: "run")`
- Old Codex PTY sessions (`dawn-shore`, `quiet-seaslug`, `delta-reef`) exited with zero output — Codex requires PTY terminal, background exec didn't attach one properly
- All engineering tasks now go to Smith going forward

### Jess v3 improvements (Smith)
- **Cool-off fix**: `_atlasOfflineCount` counter added — only triggers 2-min cool-off after 2 consecutive offline checks; resets to 0 on recovery. Committed `9823eec`
- **MC form fix**: `/r/:key` route now builds template into `let html` with try/catch — surfaces error as HTTP 500 instead of hanging. Committed `ab14fde`
- **Error reporting + pause/restart**: Jess stops poll loop and alerts Diego on Telegram if critical error (relay down 3 cycles, Gemini+Ollama all fail 3x, unhandled exception, 401 token). `/jess_restart` resumes. `/jess_status` shows state. Committed `e09d249`
- **`/jess help` command**: Added `/jess help` (also `/jess_help`, `/help`) — replies with full feature reference in Telegram. `sendTelegramMessage()` upgraded to accept `extra` options (parse_mode etc). Committed `ce094b5`

### Occupant photo feature — in progress (Smith)
- Owner wants occupant profile photo in MC detail panel
- Selfie uploaded during registration at `forms.housemates.online/r/:key`
- **Naming convention decided**: `data/tenant-photos/{waNumber}/profile.jpg` — always overwrite with latest, simple path, no version management
- Smith spawned to: add selfie upload to registration form, save as `profile.jpg`, display large circular avatar in MC occupant detail panel
- Status: in progress at session end

### Active task board
- `memory/active-tasks.md` fully rebuilt with in-flight, blocked, this-week, backlog, and completed-today sections
- Committed alongside PROTOCOLS.md in `eb8238c`
