# Memory — 2026-03-08

## Session start: ~11:30pm 7 Mar → 12:56am 8 Mar

### End-to-end registration flow — fully tested ✅
- Full flow: form submit → Telegram inline button → confirm → WA welcome package
- Tested with Diego's WA (+61416775321) and Mathis's WA (+61411590261)
- All 4 items delivered: welcome message, House Rules PDF, Occupancy Licence PDF, Natalie Mosh letter

### Bugs fixed this session

**1. TG callback offset not persisted**
- `tgLastUpdateId` reset to 0 on every MC restart → button taps consumed but not processed
- Fix: persist to `data/tg-offset.json`; read on startup
- Startup delay increased from 15s → 60s to avoid consuming callbacks during rapid restarts

**2. PDF paths wrong**
- Code pointed to `docs/` — directory doesn't exist
- Actual location: `public/docs/house-rules-2026.pdf` + `public/docs/occupancy-licence-2026.pdf`
- Fix: corrected paths in `sendWelcomePackage()`

**3. Monitor bot — 3 bugs fixed**
- `httpGet()` was using `https.get()` for localhost — all health checks returned null (false red)
- MC endpoint was `/health` (doesn't exist) → corrected to `/mc/status`
- `svcActive()` used `systemctl` without `--user` flag → checking system services, not user ones
- Jess service checked as `jess-v2` → correct name is `jess`

### Monitor bot status
- Runs as a non-systemd background process (`nohup node monitor.js`)
- PID saved at `/tmp/monitor.pid`; log at `/tmp/monitor.log`
- **Not a user systemd service** — if server reboots, monitor needs manual restart
- TODO: move to systemd user service or add to boot script

### MC commits tonight
- `db45239` — PDF paths fixed, TG offset persisted
- `0cbb44f` — TG poller 60s delay, error logging
- `dd22fef` (monitor-bot) — all 3 monitor health check bugs fixed

### Backup
- `/home/diegopalhano/backups/2026-03-07-2341` (590MB)
- GitHub pushed: workspace `c53a470`, MC `0cbb44f`, monitor-bot `dd22fef`

### System status at 12:56am
- MC: active ✅
- WA Bridge: active, CONNECTED ✅
- Jess: active (2h+ uptime, Chrome + SOCKS5 proxy running) ✅
- socks-tunnel: active ✅
- Monitor bot: 1 instance running (PID ~462476) ✅

---

## Session close — 1:01am

### Fixes applied (all committed + pushed)
- **MC quick commands**: `sudo systemctl` → `systemctl --user`; `jess-v2` → `jess`; MC `69ffdb2`
- **Monitor bot**: `httpGet` https→http fix; MC endpoint `/mc/status`; `--user` flag; `cloudflared` as system svc; single-instance lock; `dd22fef`+`62da430`
- **Monitor needs**: `sudo systemctl restart atlas-monitor` to apply code changes (requires sudo)
- **TG callback poller**: offset persisted to `data/tg-offset.json`; 60s startup delay; `0cbb44f`
- **PDF paths**: `docs/` → `public/docs/`; `db45239`

### All systems confirmed green at close
MC ✅ · WA CONNECTED ✅ · Jess active 2h+ ✅ · Cloudflare ✅ · SOCKS5 ✅

## Preference Update (DIN)
- Atlas executes directly **only** when Diego ends the command with “DIN”. Otherwise delegate to sub‑agents.
