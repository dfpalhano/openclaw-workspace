# Memory — 2026-03-06

## Codex CLI Auth Investigation

- Codex CLI v0.107.0 does NOT use API keys for auth — uses OAuth2 login
- Must run: `codex login --device-auth` then complete at openai.com/device ✅ DONE (2026-03-06)
- Previous 401 errors were NOT a credits issue — wrong auth mechanism entirely
- New project key `sk-proj-11W64nCUVvI...` was set in ~/.bashrc (old project was out of credits)
- Admin key `sk-admin-...` was also set — does NOT work for inference, only org management
- ~/.bashrc currently has new project key; ~/.codex/config.toml has `api_key` set (can remove)
- Codex reads shell snapshots (~/.codex/shell_snapshots/) which had old key baked in — that's why explicit env var passing wasn't working
- Responses API works fine via curl with new project key ✅

## OpenAI Billing
- Old project key (sk-proj-hSuAhl4oO3HA...) = out of credits
- New project key (sk-proj-11W64nCUVvI...) = valid, has credits, Responses API accessible
- Admin key saved to ~/.config/openai/admin_key

## Payments Build — Still Pending
- Full payments build waiting for explicit "good night" from owner
- Parser at ~/projects/scripts/import_rent_excel.py — needs future-date filter fix (>60 days past due = skip)
- MC routes for /mc/payments not yet built
- Bank statements: all 15 CSVs in data/bank-statements/ ✅
- Gmail labeller cron: running at 4am daily ✅
