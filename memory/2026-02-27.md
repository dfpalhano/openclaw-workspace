# Memory — 2026-02-27

## Session Summary

### Token Optimisation
- Read @mattganzak's OpenClaw Token Optimization Guide (Google Doc via Chrome relay)
- Created git checkpoint tag: `checkpoint-pre-token-opt` on GitHub before making any changes
- Plan: implement model routing (Haiku default), Ollama heartbeat, prompt caching, rate limits in SOUL.md
- Owner will notify when local Ollama install is complete

### Ollama Setup
- ollama pull llama3.2:3b had SHA256 digest mismatch errors from Ollama registry
- Fixed by clearing blob cache: `rm -rf ~/.ollama/models/blobs/*`
- Model pulled successfully after cache clear
- Local Ollama install in progress (separate from WSL/server setup)

### YouTube Summary Skill
- Owner asked about YouTube summarisation
- Found on clawhub: `youtube-iu` (YouTube Summarize), `youtube-transcript`
- Pending: inspect + scan youtube-iu before installing

### Mission Control Dashboard
- Building full ops dashboard: /home/diegopalhano/projects/mission-control/
- Files: index.html (single-file SPA), server.js (Node port 8899), plist (LaunchAgent)
- 7 tabs: Dashboard, Projects (Kanban), Timeline, Revenue, Notes, Command Center, Intel
- Login: user "diego" / pass "atlas2026" | Revenue gate: "revenue2026"
- Accent: #7C3AED purple | Theme: dark glassmorphism
- Two build attempts failed (PTY session + sub-agent). Retry pending.
- Future: Google Drive backup of site files
- Future: Cloudflare tunnel for overseas access (dashboard always-on)
- Server must survive reboots (LaunchAgent / systemd service on Linux)

### Pending Actions
- [ ] Complete Mission Control build (index.html + server.js)
- [ ] Set up systemd service for server.js (Linux, not macOS)
- [ ] Configure Cloudflare tunnel for remote access
- [ ] Install and configure Ollama locally
- [ ] Update openclaw.json: Haiku default, prompt caching, Ollama heartbeat
- [ ] Add rate limits to SOUL.md

---

## Session Update — 13:59 AEST

### Mission Control — Completed
- Full dashboard built: index.html (193KB), server.js, systemd services
- URL: https://mc.inspectionsxraytesting.com.au
- Login: diegopalhano / owner-set password (SHA-256 hashed)
- Revenue tab: separate password gate (session-only)
- Managers tab: owner password gated

### Tabs built
- 📊 Dashboard, 📋 Kanban, 📅 Timeline, 💰 Revenue, 📝 Notes
- 🏢 Command Center (agents + decisions)
- 📡 Intel feed
- 👥 Tenants (profiles, payments, bank CSV import, Excel import, ID OCR scan)
- 🏠 Houses (rooms, tenant links, weekly payment status)
- 👷 Managers (maintenance, inspections, recurring jobs + predictive scheduling)

### Infrastructure
- server.js: Node.js port 8899, Open-Meteo weather, REST API
- mission-control.service: systemd, active + enabled, auto-restart
- cloudflared.service: Cloudflare tunnel → mc.inspectionsxraytesting.com.au
  - Still fixing: SELinux blocking binary execution (203/EXEC)
  - Fix: copy binary to /usr/local/bin/cloudflared with proper context

### Fixes applied
- Weather: switched wttr.in → Open-Meteo (Brisbane lat/lon)
- Date: Invalid Date fixed — all dates now Brisbane timezone aware
- Notes: save button added alongside autosave
- Server: systemd managed, no longer dies on reboot

### Google Cloud
- Project: openclaw2-488610
- Service account authenticated
- APIs enabled: drive.googleapis.com, calendar-json.googleapis.com, vision.googleapis.com
- Drive backup: script ready at ~/projects/scripts/drive_backup.py
  - Cron: 9pm daily (0 11 * * *)
  - Pending: share Drive folder with service account email

### Pending
- [ ] Cloudflare systemd fix (SELinux — copy to /usr/local/bin)
- [ ] Share Google Drive folder with service account
- [ ] Token optimisation (openclaw.json — Haiku default, Ollama heartbeat)
- [ ] Timeroo data import for recurring job history

---

## Session Continuation — 3:25 PM

### Mission Control Dashboard — Houses & Managers Tabs

**Weather fix:** Switched from wttr.in (blocked, ECONNRESET) to Open-Meteo API (`api.open-meteo.com`) — free, no key, confirmed working (24.7°C Brisbane). Brisbane coords: `-27.4705, 153.0260`.

**Notes tab:** Added explicit `💾 Save` button (`saveNotesNow()`) alongside existing autosave. Autosave label updated to "Auto-saved".

**Houses tab (`#tab-houses`):** HTML + CSS added. Grid layout, "+ Add House" button. JS logic (CRUD, rooms, residents, payment status, maintenance list) still pending.

**Managers tab (`#tab-managers`):** HTML + CSS added. Password-gated (owner only). Sections: manager grid, maintenance jobs, inspections, recurring tracker. `checkMgrPass()` and all render functions still pending.

**Recurring jobs design:** Track history of completed jobs → compute avg interval → predict next due → snooze 7d if unbooked. Job types: grass, quick cleaning, couch cleaning, repairs.

**Timeroo API:** No public endpoint found (curl exit 35). Integration deferred.

**localStorage keys planned:** `mc.houses`, `mc.managers`, `mc.maint`

**CSS classes added:** `.pay-status-paid/waiting/missing`, `.recur-ok/warn/due`

### Remaining JS Work (Houses + Managers)
1. `openModal('house')` + house CRUD → `mc.houses`
2. `renderHouses()` — rooms, residents, rent, payment status
3. `checkMgrPass()` — owner gate
4. `openModal('manager/maint/inspection')` modals
5. `renderManagers()`, `renderMaintList()`, `renderInspections()`
6. `renderRecurringTracker()` — history, avg interval, progress bar, snooze
7. Wire tab switching for houses + managers panels

### Blocked Items (carried over)
- Drive backup: share `Atlas Mission Control` Drive folder with service account as Editor
- systemd services: user needs to run sudo cp + enable commands
- Payslip scripts: still in /tmp/, need permanent home + git commit
- Workspace git: ahead of origin by 7+ commits — needs push

---

## Delegation Pattern — 4:54 PM

**Agreed:** All dashboard/coding tasks go to Claude Code sub-agent via coding-agent skill.
- Spawn Claude Code in `/home/diegopalhano/projects/mission-control/` (background PTY)
- Atlas stays free for direction, decisions, oversight
- Only inline for: single-line fixes, config, reading files, openclaw workspace work
- Claude Code binary: `/home/diegopalhano/.local/bin/claude` — confirmed working v2.1.62
- Codex not installed — Claude Code is the coding agent
