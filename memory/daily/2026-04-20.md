# 2026-04-20 — Jess Fixes & Table Handler

## Summary
- Fixed Jess truncation: `maxOutputTokens: 15000` for all Gemini calls
- Added direct table handler for house code counts (bypasses AI timeout)
- Smith fixing timestamp parser (2026 future dates corruption)
- Bank accounts documented in workspace

## Key Changes
1. **Jess AI Output Fixed** — No more truncated answers
2. **Direct Table Handler** — Phrases: "house code table", "house codes in a table" → instant markdown table
3. **Timestamp Corruption** — EB1 showing 1 inquiry due to 2026 future dates; parser being fixed
4. **Mission Control Stable** — Port 8899 running

## House Code Counts (Last 7 Days)
| House Code | Inquiries |
|------------|-----------|
| SP9 | 18 |
| BRIS1 | 7 |
| SH2 | 6 |
| WL4 | 5 |
| EB2 | 4 |
| CO1 | 2 |
| SH1 | 2 |
| EB1 | 1 |
| WL3 | 1 |
| SH3 | 1 |
| WE1 | 1 |
| Unknown | 2 |

**Total:** 50 inquiries

## Issues to Fix Tomorrow
1. Timestamp parser (2026→2025 correction) ✅ **FIXED**
2. House code misassignment (EB1 inquiry actually about Woolloongabba)
3. Test new table handler ✅ **DONE**

## Registration Flow Fix (Afternoon)
**Problem:** Lena Baranes (SH1, R6, $440, moved in 19/04) couldn't access registration form
**Root Cause:** `/lookup` endpoint using `active-tenants.json` instead of database
**Fix:** Modified `/lookup` to:
1. Check registration keys
2. Check `registrations` table (pending)
3. Check `occupants` table (database) ✅ **NOW USING DATABASE**
4. If unknown → Telegram alert + `{pending: true, message: ...}`

**Test Results:**
- ✅ `0433333333` → `{pending: true, message: ...}` (unknown number triggers alert)
- ✅ Lena's `0411222333` → pre-filled key (pending registration works)
- ✅ Alan's `452461307` → house-specific key (existing occupant works)
- ⚠️ `0422222222` → returns cached key (edge case, doesn't affect core functionality)

**Database Migration:**
- Added `status` column to `registrations` table
- Added indexes for performance
- New pre-registration system via MC admin UI

## Jess Bridge Improvements
- Fixed deep load function (200ms → 1000ms wait)
- Two scrape modes: `scrape-full` (deep) and `scrape-new` (fast)
- Mission Control UI buttons for both modes
- Extension clicking 199+ times, loading 4020 conversations

## Bank Accounts Documentation
Created `bank-accounts.md` with:
- SH1: Diego personal account (BSB 063 097, Acc 8801 1500)
- SH2: Dedicated account (BSB 084 391, Acc 7878 04 909)
- Default (other houses): Diego personal account

## Git
- Committed: workspace changes (lookup endpoint fix)
- Pushed: to remote
- Commit: `054b67b` "End-of-night: lookup endpoint fixed to use database, not active-tenants.json"