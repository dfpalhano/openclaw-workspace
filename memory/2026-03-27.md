# 2026-03-27

- Google Calendar integration diagnosis: local OpenClaw `google-calendar` skill wrapper in this runtime is not configured, but this does not prove the wider system lacks calendar integration; likely production calendar creation exists in Mission Control or another service path and should be traced before any fresh setup.
- Distinction to preserve: missing local env/config for the standalone skill is different from missing calendar support across the stack.
- Next investigation path established: locate the real service/code path that creates existing inspection calendar events, identify its config/token source, and determine why the current session/runtime could not access that path.
- Operational rule locked: formal property inspections use `[INSP] <HOUSE_CODE> <TYPE>` and are distinct from Jess room viewings/applicant inspections.
- Operational rule locked for Jess calendar bookings: use `[VIEWING] <HOUSE_CODE> <room> <price> <assistant>`.
- Team memory: Emilio is no longer in the team and should not be used as an active assistant/manager in new bookings or operational routing.
- Smith implemented Jess viewing calendar support in Mission Control: new `POST /mc/calendar/viewing-event` route in `server.js`, with title format locked to `[VIEWING] <HOUSE_CODE> <room> <price> <assistant>`. `[VIEWING]` is now permanently separate from `[INSP]` formal inspection events. Commit: `5f0e119`.
- Viewing-path cleanup: Emilio removed from assistant handling/defaults in the relevant Mission Control viewing/booking path; `dee` display name corrected to `Dee`.
- Vacancy-check procedure locked: when asked which rooms are empty or going to be empty, use this order — (1) Mission Control occupant data, (2) private WhatsApp messages for leave/move-out confirmation and dates, (3) manager confirmation only if still unclear, and treat `jess-rooms.json` as marketing-only, never occupancy truth.
- Output preference locked for vacancy answers: clean WhatsApp-style sections using `EMPTY NOW`, `LEAVING SOON`, `NOT VACANT`, and `NEEDS CONFIRMATION`; avoid noisy raw dumps.
- Vacancy truth corrections from owner in this session: `SH1 R3` is taken; `SP9 R2` is empty; owner confirmed `CO1 R6` empty, `SH3 R5` will be empty, `SH1 R5` empty, `SH1 R1` will be empty soon, and `EB3 R2` will be empty soon. These should be verified against MC + private WA before final vacancy lists are presented.
