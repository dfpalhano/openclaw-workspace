# Memory — 2026-03-13

## paydb Updates (late 12/03 → early 13/03)

### Transactions Tab (commit `8c3586e`)
- New top-level "Transactions" tab in paydb
- Backend: `GET /mc/paydb/transactions` — filters: house, status, from, to, page, q
- 100 rows/page, pagination, inline edit per row
- WL4 test: 84 transactions ✅

### Bond Returns — Detail Modal (commit `bae9ee9`)
- "View Details" button per row → modal with full submission info, bond/rent payment details, contact, shared bond section
- Close with ×, outside click, or Escape

### Bond Returns — Transactions Button (commit `5bf4bcb`)
- "Transactions" button per row → payment history modal (editable inline)
- Backend: `GET /mc/paydb/bond-returns/:id/transactions` — fuzzy match on first word of name + house_code

### House Tenant List — MC as Source of Truth (commit `5bf4bcb`)
- House view now driven by MC `tenants.json` (not stale DB list)
- WL4: 5 active, 4 archived
- Tenants with no DB match still shown; click → "No payment records in database"

### tenants.json Deduplication
- 736 → 375 records (361 duplicates removed)
- Merge logic: same name + houseCode → keep best record; real AU phone = primary, LID stored as `lid` field
- Active tenants: 109
- Backup: `tenants.json.bak.20260313_005039`
- Arnold SH1: now 1 record — phone `61461366402`, lid `115561010831411`

### Bond Returns — Benjamin Dubois + Egon EB2
- Both marked `paid` in bond-return-requests.json (paid by Diego 12/03/2026)
- Auto-clears in 7 days

---

## EB2 Room Status (confirmed 13/03)
- R1 (ensuite) → James Murray + Ioanna leaving Sat 14/03 → Colin + Pablo move in 14/03 ✅
- R7 → Tom Ross (moved in 12/03) ✅
- R4 (garage) → Mathias leaving → Maxime replacing ($200/wk, shared with Hugo)
  - Invitation timer set: sends 7:45am Fri 13/03 AEST to `33623462916@c.us`, reg key `vqs2pp2Fpb`
- **EB2 is full** — Diego confirmed. Deactivate Jess EB2 listing pending (Chrome extension needed)

---

## Jess Status
- PID: 2840409 (restarted during session)
- Running ✅ but in quiet hours until 7:30am (skipping poll cycles, not sending)
- `/jess deactivate EB2` — command ready; requires Chrome extension + Flatmates tab open
- Autoscrape: Jess polls inbox in active hours; quiet hours gates sending only

---

## Reminders for next session
- Remove James + Ioanna from EB2 group: Sat 14/03 ~11am (Rule 23)
- Add Colin + Pablo to EB2 group after James/Ioanna removed
- Send Mathias Vinière vacate checklist once Maxime registers + pays
- Elage WL4: $147 due Monday 16/03 morning (ref `44750554228 WL4`) — enforce if unpaid
- SH2 inspection today Fri 13/03 10am–2pm — monitor who shows up
- Swan EB1 cron — verify fires Fri 13/03 09:00 AEST
- WL4 rubbish deadline: Thu 13 Mar ~20:40 AEST — check if cleared
- Chloe + Dylan SH1 R5 — awaiting form submissions (keys `VO86Ombv3a`, `bvpBpv0nBw`) + payment
- Tom Ross, Colin, Pablo EB2 — awaiting form submissions
- Thomas SH2 — awaiting form submission (key `G1TJarY68C`)
- Nathan + Mariane WL4 — awaiting form submissions
