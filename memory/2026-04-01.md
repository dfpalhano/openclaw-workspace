# Memory — 2026-04-01

- Bond-return tracking should be attached to the occupant record lifecycle, not treated as a separate identity chain.
- Main anchors: WhatsApp number for the person, occupant/tenant id for the stay, bond return as a child case linked to the occupant record.
- Bond-return request ids and token ids are workflow helpers only; they are not source-of-truth identifiers.
- For occupancy drafts, the commencement date must always be the actual calendar date, not "tomorrow".
- For occupancy messages, the bond due now structure should include bond + first week where applicable.
- Maxime meys and Louis inda-gallur bond-return records were treated as paid/archived and should not reappear in pending bond lists.
- Pending bond-return lists should be filtered from active/open linked cases only, not raw tokens or unlinked requests.
- If a paid bond-return item reappears, that indicates the source record or filter is still wrong and needs fixing at the data layer.
- 2026-04-01: Requested a one-time 8am Brisbane cron send for Mathilde bond return. Use the MC bond-return template with pages 20–24 reference and couple/shared-room cleaning fee rules.
- Messaging confirmation rule updated: single destination (1:1 or one group) executes without extra confirmation when the instruction is clear; multiple groups/destinations, ambiguous details, confusing corrections, or unusually sensitive/public-facing messages require extra confirmation.
- WhatsApp bridge /send-file requires `filePath` (not `path`) in the JSON payload. Use `to`, `filePath`, optional `caption`, and optional `filename`. This is the working permanent send-file format.
