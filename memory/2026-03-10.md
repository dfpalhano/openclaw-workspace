---
hyperspell_id: 0Lq9nTxAEXL3Cw
---
# Daily Memory — 2026-03-10 (Tuesday)

## Session 1 (early morning, pre-compaction)
- Vox 17 rules approved and live (commit `2ca40f2`)
- Vox audit logging added (commit `6e9beee`) — needs sudo restart to pick up
- Stackd Ops spec saved and logos committed
- Payment reconciliation run: paid:34, recent:12, late:8, overdue:6, no_payment_found:56
- Oliver warning message: HELD overnight, to send 9am Tue 10 Mar

---

## Session 2 (09:00–10:04am)

### Kimi K2.5 — LIVE ✅
- Moonshot API key added to `~/.openclaw/openclaw.json` via `read -s` terminal method
- Key confirmed: length 51, prefix `sk-eMV`
- Model tested and responding as `kimi-k2.5`
- SOUL.md updated: Kimi now routes multilingual, Vox, Jess, large-context tasks
- Commits: `22456ad` (PROTOCOLS Rule 11), `2aab9be` (SOUL.md routing)

### Protocol Rule 11 — Credential Input (LOCKED)
- **Primary:** `read -s` terminal command — never via chat
- **Fallback:** Confidant with Tailscale IP `100.92.117.73` + tunnel password always included
- Tunnel password for localtunnel: always provide `118.208.196.152` alongside any loca.lt link
- Committed to PROTOCOLS.md: `22456ad`

### Bridge ↔ Vox Bidirectional Integration — Smith delivered ✅
Full build completed:

**Bridge new endpoints (127.0.0.1 only):**
- `GET /chats?limit=200` — inbox scanner, cross-refs active-tenants.json
- `GET /chats/:jid/messages?limit=50` — message history per chat
- `POST /chats/analyse` — keyword scan: notices, substitutions, inquiries, unknownContacts, missingFromGroups, actionItems
- `GET /contacts/resolve/:phone` — WA identity lookup
- `GET /groups` — all groups with participant list
- `POST /groups/:groupJid/add` — executor only (called by scheduler)
- `POST /groups/:groupJid/remove` — executor only (called by scheduler)

**Vox new files:**
- `lib/bridge.js` — thin client for all bridge calls
- `lib/group-schedule.js` — date-gated group membership scheduler
- `data/group-schedule.json` — pending add/remove actions

**Group membership rules (LOCKED):**
- Add to group: only on or after move-in date
- Remove from group: only on or after leave date
- Early remove requires `confirmed=true` override
- Scheduler runs: **immediately on startup today (2026-03-10)**, then **8am Brisbane daily**
- `lastRunDate` tracked in group-schedule.json to prevent double-runs

### WA Inbox Scan — context understood
- Owner wants personal DM scan (not group exports)
- Bridge `/chats/analyse` now provides this — fetches last 200 DMs, cross-refs MC, keyword scans
- Non-occupant contacts (last 10) also surfaced as potential new occupants

### Occupant Messages Sent Today
- **Max SH3** (`33676746755`) — bond form bug fix message ✅
- **Valentin Chaussier SH3** (`33614492677`) — bond form bug fix message ✅
- **Oliver GS1** (`61422142791`) — warning message sent (hygiene/downstairs toilet restriction/room inspection) ✅

### Oliver GS1 — number now in MC
- Phone: `+61422142791`
- Added to `active-tenants.json` (was missing)

### Max and Valentin SH3 — bond form issue
- Same `HAS_SUBMISSION_ATTEMPT` bug as Blair/Chloé — fixed in commit `0d96d09`
- Both messaged to refresh and retry

---

## Pending / In Progress
- Vox service needs `sudo systemctl restart wa-ops-bot` to pick up audit logging (`6e9beee`)
- `bridge.scheduleGroupChange()` not yet wired into `replacement_screening.js` flow (separate task)
- Blair: refreshed and re-attempted? Outcome unconfirmed
- Noemie, Malena, Jamie's friend: awaiting replies
- WL4 MC notes/warnings for Charlene/Emma: approved conceptually, not yet executed
- Payment reconciliation re-run: waiting on new bank/Excel data from Diego
- Jess quiet hours: still disabled (hardcoded `false`, commit `22ab87c`) — re-enable after testing
- `/chats/analyse` vacancy inference: keyword-based only; LLM upgrade is future work
- Old `/group/add` and `/group/remove` bridge endpoints still exist (backward compat) — flows should use `bridge.scheduleGroupChange()` exclusively

---

## Session 3 (11:30am–12:00pm)

### Mission Control — IPv6 Telegram spam fixed ✅
- MC's `telegramRequest()` was missing `family: 4` → IPv6 ETIMEDOUT every 5s, flooding logs and slowing event loop
- Fix: added `family: 4` to all MC HTTPS options (same fix as Vox had)
- MC restarted cleanly — no more TG getUpdates errors
- **Note:** MC was also restarted at 08:10 by Thor writing to server.js (expected, one-time)

### Arnold SH1 — Leaving 2026-03-11 ✅
- Paid $50 for last night
- Bond return form was failing: generic form requires BOTH bond + rent payment sections; Arnold only filled bond
- **Resolution:** Bond return manually entered to MC by Atlas
  - ID: `br_19cd567261d_manual`
  - Bank: BSB193879, Acc 442267431
  - Status → `bond_pending`, moveOutDate `2026-03-11`
  - Notes: "Bond amount TBC — check SH1 records"
- WA sent to Arnold: confirmation + collecting email + confirming move-out date
- Vox thread created for Arnold: `bond_return_followup` stage, `approvedAutoFlow: true`
- **Still needed:** Arnold's actual bond amount and weekly rent (currently $0 placeholder)
- SH1 group removal (Springing Love 💖 Rosa St) not yet scheduled — pending Diego confirm

### Registration Reminder System — Thor dispatched ✅
- Thor running in background (session `dawn-nudibranch`, PID 1121354)
- Working dir: `/home/diegopalhano/projects/mission-control/`
- Brief saved: `/tmp/thor-brief.txt`
- Tasks assigned:
  1. `MC/scripts/registration-reminders.js` — daily blast, 3-tier escalation (Day 1 friendly / Day 3 firm / Day 7 formal)
  2. `GET /api/registration-status` + MC Occupants badge (✅ ⏳ ❌)
  3. Vox registration reply handler with owner escalation option
  4. First blast: send immediately to 31 occupants with keys but no submission
- **Status:** Still running as of 12:00pm — awaiting auto-notify on completion

### Bond Return Form — UX Bug flagged
- Generic form UX issue: rent payment section not visible/clear → occupants submit without it → 400 error
- Fix not yet done — queued to flag for Smith (non-urgent)

### SP9 Viewing — Tonight 7pm ✅
- **Emilio** (`61481517377`) messaged via WA bridge — asked if available for SP9 showing tonight at 7pm
- **Jess** — 12 SP9 leads messaged via Flatmates send queue:
  - Matt, Naiara, Catalina, Marco, Erin, Loll, Isha Pancholi, Sergio, Nikita, Maurice, Apo, Serena
  - Message: "Are you free for a viewing tonight at 7pm in Surfers Paradise?" — no house code, no address, no robot mention
- **SP9 lead list saved:** `MC/data/sp9-viewing-list.json` — tracks name, phone, status, viewingResponse
- **Process:** Positive reply → flag to Diego → Emilio confirms → Diego approves → send address + Emilio contact
- Commit: `af321b4`

### Vox — Arnold thread setup
- Thread type: `bond_return_followup`, stage: `collecting_email`
- Collecting: email + move-out date confirmation
- If occupant replies "DIEGO" → escalate immediately

### Jess Flatmates Rules (confirmed by Diego)
- **NEVER** mention house code in Flatmates messages
- **NEVER** reveal it's a bot/automated system
- First contact = availability check only (no address)
- Positive → hold → Emilio confirms → Diego approves → then send address + contact

---

## Session 4 — Afternoon (~1:20–1:32pm AEST)

### Vox — LID Contact Send Fixes (2026-03-10 ~1:20pm)
- **Root cause confirmed:** `sendWA()` was appending `@c.us` to all contacts; LID contacts (15-digit IDs) need `@lid` suffix
- **Fix 1 (tg-poller.js `sendWA`):** Detect LID via `/^\d{14,}$/` → use `@lid`; confirmed via `curl` bridge test returning `{"ok":true}` for Hugo + Laura
- **Fix 2 (tg-poller.js `finaliseSend`):** Bridge resolves (doesn't reject) on error → `finaliseSend` logged OUTBOUND even when send failed silently. Now checks `result?.ok` explicitly; sends `❌ Bridge rejected send to <display>: <error>` to Telegram on failure
- **Fix 3 (Raphaël WL4 whitelist):** LID `179727872573549` had `isKnownOccupant: false, houseCode: ''` — updated to `isKnownOccupant: true, houseCode: 'WL4'`. Vox was firing unknown contact alert for him
- Service restarted: PID `1167267` (since 13:27:32 AEST)
- Commits: `57c2593` (LID sendWA fix), `2a5d53a` (finaliseSend check + Raphaël whitelist)

### Hugo (LID 160310476468297) — EB2 occupant candidate
- Raphaël's friend, asking about a double room at $430/week for himself
- Messages after Vox restart (13:23, 13:25): apologised for accidental call; confirmed he's looking for a double room solo at $430/week
- Thread type: `registration_recovery / init`, watch-only (no auto-flow approved)
- Previous OUTBOUND message at 03:15 silently failed (bridge got `@c.us`, rejected) — message never received
- Diego still needs to provide what to say; tg-pending cleared

### Raphaël (WL4, LID 179727872573549)
- Messaged Vox: "I want to make the payment but they said this" (13:26 AEST)
- Was incorrectly flagged as UNKNOWN CONTACT — now fixed in whitelist
- Payment issue: occupant trying to pay but hitting an error — needs follow-up from Diego

### Laura Lecchi (LID 19293681012763) — replacement screening
- Previous OUTBOUND at 01:52 silently failed (same LID bug)
- Thread: `replacement_screening / profile_collection`, approvedAutoFlow: true
- Message sent: "Hi Laura…could you tell me a bit about yourself…" — not received
- Resend needed; house still unknown (Mathilde + Valentine's room, identity TBD)

### Key Paths (confirmed active)
- Vox: `/home/diegopalhano/projects/wa-ops-bot/` — PID `1167267`, ports 8891/8892
- Fix applied: `lib/tg-poller.js` — both `sendWA()` and `finaliseSend()`
- Whitelist: `data/staff-whitelist.json` — Raphaël LID now correct

---

## Session 5 — Afternoon (~3:20–4:02pm AEST)

### Smith delivery — Jess MC panel rebuilt
- Smith sub-agent completed: Jess MC panel at `/mc/jess` fully rebuilt
- New design: scraper-only (no auto-replies), every Flatmates lead shown as a card
- Each card: house, room, name, message summary, Generate Reply button
- Generate → Gemini drafts → editable textarea → Diego clicks Send
- Learning loop: every edit stored by house, improves future drafts
- `jess-v3.js` stripped of auto-reply logic, feed-only scraper pushing to MC

### Vox — WA Reaction Fix (2026-03-10 ~3:03pm)
- Bug: WA 👍 reaction on a message came back through bridge as inbound event
- Vox processed it as a message from Laura SH2 → triggered auto-flow
- Fix: reactions now silently ignored in `index.js` (type === 'reaction' OR body starts with '[reaction:')
- Commit: `5ce608d`
- Service restarted: PID `1215296` (since 15:03:08 AEST)

### Jess AI Model Fixes (2026-03-10 ~3:07–3:15pm)
- Root causes identified:
  1. Length gate `> 800` chars — some Gemini replies for complex convos exceeded this → "bad output"
  2. Kimi returns "Or: ..." multi-option format + `$[rent]` unfilled placeholders → "bad output"
  3. `gemini-2.5-flash` is preview/unstable
- Fixes applied to `jess-v3.js`:
  - Primary model changed: `gemini-2.5-flash` → `gemini-2.0-flash` (stable)
  - Length gates widened: `> 30 && < 800` → `> 10 && < 1500`
  - Quality filter added: strips "Or:" separators and unfilled `$[variable]` placeholders
- Commit: `7fc4d8a`
- Jess restarted (old PID 1150399 killed, new PID 1243429) via SIGTERM → systemd auto-restart

### Monitor Bot — /add command upgrade (2026-03-10 ~3:20pm)
- `/add <number> <house>` now shows registration message preview + inline Telegram buttons
- Button 1: "📲 Send via WA" → fires message directly through WA bridge to the number
- Button 2: "✋ Skip" → dismisses, nothing sent
- WA bridge send: uses LID detection heuristic (≥14 digits → `@lid`, else `@c.us`)
- `tgAnswer()` helper added for callback query acknowledgment
- `waBridgeSend()` helper added (http to 127.0.0.1:8890/send)
- Commit: `c345674`
- Service restarted: new PID `1239499` (HUP + systemd auto-restart at 15:23:44)

### Registration message template (locked by Diego)
```
Hey! 😊 Welcome to the house. To complete your move-in, please fill out your occupant registration form — it only takes a few minutes:

https://forms.housemates.online/r/<KEY>

You'll need:
• A photo of your passport or driver's licence
• Your bank details (for rent payments)
• A quick selfie

Let me know if you have any questions! 🙌
```

### Blair BRIS1 — Vox registration_recovery flow (2026-03-10 ~3:40pm)
- Blair: LID `45158007607367`, reg key `a-AWXKS9tg`, form not submitted
- Previous thread: `registration_recovery / init`, watchOnly: true, approvedAutoFlow: false
- Thread updated: watchOnly: false, approvedAutoFlow: true, stage: awaiting_response
- Opening message sent via bridge: check-in + registration link + offer to speak with owner
- `registration_recovery.js` fully implemented (was stub):
  - `awaiting_response`: done/submitted → confirm ✅ | trouble/error → resend link | owner request → escalate
  - `troubleshooting`: still broken → escalate to Diego
- tg-pending entry 48 (Blair stale draft) cleared
- Commit: `f6dbfcf`

### Vox — Whitelist Permanent Fix (2026-03-10 ~3:43pm)
- Root cause: whitelist only read `active-tenants.json` → Blair, Mathis, other LID contacts not in it → Rule 17 hard-stop
- Permanent fix: `loadStaffWhitelist()` now reads from ALL 6 sources:
  1. `staff-whitelist.json`
  2. `active-tenants.json`
  3. `tenants.json` ← Blair + Mathis live here
  4. `residents.json` ← WA export with LIDs
  5. `tenant-crossref.json`
  6. `registration-keys.json` (anyone assigned a key = known occupant)
- Rule 17 handler updated: if contact found in expanded whitelist → auto-thread + approvedAutoFlow: true (known occupant path); if truly unknown → Rule 17 hard-stop still applies
- Commit: `978a711`
- Service restarted: PID `1246038` (since 15:42:28 AEST)

### Open Items (end of Session 5)
- Jess quiet hours still hardcoded `false` (commit `22ab87c`) — re-enable after testing
- SP9 12 leads still stuck as `viewing_pending` — Jess last_3_days filter may exclude older convos
- Laura SH2 reply not yet pasted by Diego
- Laura Lecchi bond/couple-rate message not confirmed for resend
- Forge TG bot token not yet created by Diego
- Raphaël WL4 payment issue needs Diego response
- Arnold SH1 group removal scheduled 2026-03-11
- wa-ops-bot system unit still crashing (user unit PID 1246038 is the active one)

---

## Session 6 — 2026-03-10 ~16:30–16:57 AEST

### Inspection Scheduling Flow
- Built and saved `projects/inspection-scheduling-flow.md` (commit `6427575`)
- Rules locked:
  - Min 2-hour lead time from Jess Flatmates message to inspection
  - ~20 min per house + travel time
  - Stacked max ~4-5 houses/day
  - Diego must approve schedule before Jess sends any messages
  - Emilio = SP9 + Gold Coast; Mathis = Brisbane houses only

### Jess: All-Houses Scraping + Lead Filtering
- `jess-rooms.json` created: all 15 houses with full addresses + vacant rooms (commit `df56a00`)
- Filter expanded: `last_3_days` → `last_30_days`, all houses
- `jess-filter.json` written to `jess-bot/data/`
- Completion ping added: after each poll cycle, Jess pings Diego via `@jess_flatmatesbot` with active leads per vacant house (filtered: online ≥ yesterday)
- `lastActive` field now stored in `jess-enquirers.json` (both new + update paths)
- `cutoffDays` map expanded to include `last_30_days: 30` and `last_14_days: 14`
- Commit: `5326d9e` | Jess PID: `1269802`

### MC Restart Required (Fixed)
- Root cause of `MC jess/fetch returned 404`: MC PID `1195691` started at 14:14 AEST, before Smith's `/mc/jess/fetch` route was committed (~15:20)
- Fix: `kill -HUP 1195691` → MC restarted as PID `1272931` (since 16:54 AEST)
- Verified: `POST /mc/jess/fetch` now returns `200 {ok:true, newCount:120, total:120}`
- 120 Flatmates conversations pushed to MC inbox on first hit

### Vox: NEVER SILENT Fix (Raphaël + "Already handled" bug)
- Bug: NEVER SILENT alert pinged Diego but created no tg-pending entry → replying "Sure" hit "Already handled"
- Fix: watchOnly inbound now generates Kimi draft AND creates tg-pending entry
- Diego can reply "Sure" → message fires; "skip" → dismissed; any edit → Kimi refines
- Commit: `d1a6a05`
- Raphaël sent: "Of course! 😊 Go ahead and give him my number..."

### Vox: Mathis Thread Unlocked
- Thread `61411590261`: stage `scheduling`, status `active`, `approvedAutoFlow: true`
- Sent Mathis available room list: CO1 R4, EB2 R1+R7, EB3 R1+R3 (SP9 = Emilio)
- Waiting for Mathis to reply which houses he wants to inspect

### Vox: Kinan WL4 Thread Created
- Phone: `32487803886@c.us` (Belgian number, standard E.164, NOT LID)
- Was staying in Raphaël's room at WL4; wants Victor's room from Monday 16 Mar
- Thread: `replacement_screening`, stage `complete`, `approvedAutoFlow: true`, `houseCode: WL4`
- Reg key `9xMHqwdT8R` (WL4 slot 2, already assigned to his number) — form link sent
- Opening message sent with form + option to speak to Diego
- Commit: `1d6c69e`

### Open Items (end of Session 6)
- Jess completion ping: will fire within next 2 min cycle — await lead counts for CO1/EB2/EB3/WL4/SH2
- Mathis: awaiting reply on which houses to inspect
- Kinan: awaiting registration form submission (`9xMHqwdT8R`)
- Jess quiet hours still hardcoded `false` — re-enable after testing
- Forge TG bot token not yet created
- Arnold SH1 group removal: 2026-03-11
- Raphaël WL4 payment issue: pending Diego response
- 31 occupants assigned keys but not submitted — individual reminders still pending
- `jess-v3` filter now `last_30_days` — SP9 12 leads should send on next Jess cycle

---

## Session 7 — 2026-03-10 ~17:00–18:25 AEST

### New Hard Rules (LOCKED TODAY)
- **Draft-first rule (17:47 AEST):** ALWAYS show WA draft to Diego before sending. No exceptions. Every message, every contact, every situation. Violated with Swan — locked permanently.
- **Thread lifecycle:** Close thread when resolved. No lingering watchOnly. Resolved = closed.
- **Active tenant detection (commit b19ecaa):** Contacts in `active-tenants.json` → watchOnly + owner ping ONLY. Never auto-flow into registration_recovery. Was causing Lilian GS1 to be spammed about forms she doesn't need.

### Vox Fixes
- `index.js` fixed: `isActiveTenant` check before auto-threading (commit `b19ecaa`)
- Stuck tg-pending cleared: null/refining draft entries cause "Already handled" loop — cleared manually when Diego accidentally sent `/add` command into Vox reply
- Vox restarted: PID `1298624`

### Inspections Scheduled
- **SH2** — Fri 13/03, 10am–2pm | Template sent to group (`120363200964128944@g.us`) | Emilio asked to confirm | Calendar event created (Diego's primary calendar)
- **EB2** — Fri 13/03 | James Murray confirmed ("Yes no worries") | Thread closed
- **SH1** — Tonight 7pm | 10 Flatmates leads notified | Mathis host
- **CO1** — Tonight 7:30pm | 4 Flatmates leads notified | Mathis host

### Tenants Confirmed / Progressing
- **Swan Landais EB1** — Confirmed for room (beat other person). Payment details sent: BSB 014 002, Acc 231 444 039, Ref `1519870741 EB1`, $330/wk, move-in 14 Mar
- **Kinan (32487803886)** — WL4 Victor's room, move-in Mon 16 Mar, reg key `9xMHqwdT8R` sent
- **James Murray EB2** — Confirmed inspection access Fri 13/03. Thread closed.

### Jess
- 120 Flatmates conversations loaded after MC restart
- Lead counts: SH1=10, EB2=9, CO1=4 (active in last 24h from jess-inbox.json)
- EB3 reported FULL by Mathis — awaiting names of new residents
- Jess completion ping now fires after each cycle with active lead counts per house

### Key Data Sources (things I keep forgetting)
- **Lead counts:** `jess-inbox.json` (NOT jess-enquirers.json — that's mostly empty)
- **Bank details per house:** `house-bank-accounts.json` → `houses.{CODE}.bsb + account`
- **Swan's payment ref:** `registration-keys.json` → `paymentReference` field
- **Active tenants:** `active-tenants.json` — always check here first before auto-threading
- **MC restarts needed:** after any new server.js commit — MC won't load new routes until restarted

### Open Threads (end of session)
| Who | Phone | Waiting for |
|-----|-------|-------------|
| Emilio | 178289075347526@lid | SH2 Fri confirm |
| Swan | 33667498283@c.us | Payment receipt / move-in |
| Mathis | 61411590261@c.us | EB3 new resident names |
| Laura Lecchi | 19293681012763@lid | Form questions (watching) |
| Blair | 45158007607367@lid | Registration (escalated) |
| Laura SH2 | 61412841595@c.us | Reg form submission |
| Noémie WL4 | 107941202002047@lid | Move-in date |
| Kinan WL4 | 32487803886@c.us | Reg form submission |
| Lilian GS1 | 33602265516@c.us | Diego to reply re: friend room-share $390 |

---

## Session 8 — 19:15–19:23 AEST

### Swan EB1 — Onboarding Complete ✅
- Payment confirmed. Active tenant record updated.
- Welcome package sent directly to Swan (`33667498283@c.us`):
  1. Welcome message
  2. House Rules PDF
  3. Occupancy Licence PDF
  4. Natalie Mosh letter (generated)
- Cron set: **Fri 13 Mar 09:00 AEST** → add Swan to EB1 group (`120363403136537362@g.us`) + post group welcome message
- Script: `/home/diegopalhano/projects/scripts/swan_eb1_group_add.sh`
- Cron entry: `0 23 12 3 * /home/diegopalhano/projects/scripts/swan_eb1_group_add.sh`
- Group welcome uses template: Swan from France, downstairs queen room, 🇫🇷

### Bond Return Form — Bugs Fixed (commit `ce8fbbf`)
- **Bug 1:** `checkEmailMX()` — DNS MX lookup was blocking submissions on timeout → now fail-open (allow if DNS fails, still reject malformed domains)
- **Bug 2:** `!payload.bondPayment?.amount` failing for empty string `""` → fixed to `!String(val ?? '').trim()`
- MC restarted after fix

### Chloé André — V5 Bond Return
- She submitted **15+ times** over 2 days — the form WAS working server-side, data landed, but success screen never showed (no localStorage set on error)
- All entries in `bond-return-requests.json`; best entry has receipt PDF attached
- Phone: `33771165221` (French), AU alias: `61432507369`
- Bond: $1,200 | Rent: $480/wk | Move-out: 2026-03-01
- Payment: Revolut, Chloé Pauline André / Hugo Patrick Pain, last 4: 2437
- **Pending Diego approval** to send reassurance message + deduplicate 15 entries in MC

### Maxime (WL4) — Shared Bond Return Bug
- Reported form error when selecting "Yes — I shared bond with someone else"
- Root cause: same MX + empty string bugs as Chloé (now fixed)
- **Pending Diego approval** to send "try again" message to Maxime

### Lessons
- Bond return form had silent success-screen bug: server saved data but client showed error → tenants kept resubmitting. Fix: always set localStorage on server 200, even if the screen update fails.
- `checkEmailMX` DNS lookup is fragile — always fail-open on SERVFAIL/TIMEOUT; only reject on clear format errors.

---

## Joshua SP9 R3 — Girlfriend Tracking Flag
- Phone: `61493598438@c.us`
- Reg key: `W9eWM7GY` — SP9, Room 3, $310/wk
- **⚠️ Girlfriend arriving from Germany** — rate increases to **$400/wk** when she joins
- Need to confirm her arrival date and update rent accordingly
- Invitation sent 2026-03-10 19:38 AEST

---

## Session 9 — 20:05–20:26 AEST

### Registrations Panel — "Could not load registration" fix (commit `c8d4fea`)
- Root cause: POST `/r/:key` writes to `registration-keys.json` but was NOT reliably writing to `resident-registrations.json`
- 36 registrations were missing from `resident-registrations.json` — migrated via Python script
- `GET /mc/registrations/:id` now falls back to `registration-keys.json` if not found in resident-registrations, and auto-migrates on the fly
- Total registrations now: 38

### WA Bridge — silent unknown number fix (commit `fd15862` in whatsapp-bridge)
- Root cause: bridge was auto-replying "Your number is not yet in our system..." to any unknown number asking for a reg key
- Removed the auto-reply entirely — Diego gets a silent Telegram ping only
- Guillaume Pech (EB2, `85375510929662@lid`) was triggering this — his thread stopped, watchOnly=True, houseCode=EB2

### Guillaume Pech EB2
- Had no reg key assigned
- Reg key `Kbcc8bP7` created for EB2, link sent to `85375510929662@lid`
- Thread: stopped automation, watchOnly, houseCode=EB2

### Max & Valentin SH3 — form fixed nudge sent
- Max (`33676746755@c.us`) and Valentin (`33614492677@c.us`) both sent message to retry the registration form

### Jess fixes
- `pollTelegramCommands()` was defined but never wired in — @jess_flatmatesbot was completely deaf (commit `9b8cd24`)
- `RELAY_PORT` undefined in `checkRelayHealth` — should use `RELAY_URL` constant (commit `f8a6ec8`)
- `/rj` in monitor bot used `sudo systemctl restart jess-v3` which requires password → fixed to kill+respawn without sudo (commit `3520ad4`)
- Stale relay command queue cleared (command ID 1306, was stuck since 09:56 AEST)
- Jess PID: `1352693` (latest after relay fix)

### Vox fix — direct TG commands silently dropped (commit `47e7f0f`)
- `handleOwnerReply` had `if (!replyTo) return` early — any direct message to @vox_wa_messages_bot not a reply was silently dropped
- Fixed: top-level command handling before reply guard (`/status`, `/vox_status`, monitor-bot redirect)

### Relay status (20:26 AEST)
- Extension connected ✅, heartbeat live
- Last actual Flatmates scrape: 41 hours ago (2026-03-08T17:21:20.481Z) ❌
- Requires Diego to open Chrome → flatmates.com.au inbox with extension active
- 26 pending Jess messages will fire once scrape is fresh

### Registrations panel overhaul — delegated to Codex (session `kind-canyon`)
- Task: profile modal on card click, edit mode, confirm inside modal only, reject → archive, new occupant triggers WA welcome, existing occupant does not
- Codex running in background — will notify on completion

### Git commits this session
- `c8d4fea` — MC: migrate 36 missing registrations; detail endpoint falls back to reg-keys
- `fd15862` — WA bridge: remove auto-reply to unknown reg key requests
- `9b8cd24` — Jess: wire pollTelegramCommands into main loop (was never called)
- `f8a6ec8` — Jess: fix RELAY_PORT undefined → use RELAY_URL
- `d4ed28d` — Jess: relay health gate + post-send verification + send_failed retry
- `47e7f0f` — Vox: handle direct TG commands before reply guard
- `3520ad4` — Monitor bot: fix /rj to kill+respawn without sudo
