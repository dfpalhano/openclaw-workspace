# Memory — 2026-03-07 (Saturday)

## Session 1 (morning, continued from 6 Mar)

### Orbit PM Monitoring
- Added `pollPMs()` to orbit.js — monitors all PM chats every 20s for `/orbit` or `Internal:` commands sent by Diego (fromMe)
- `Internal:` prefix now treated identically to `/orbit` trigger
- `DIEGO_WA = '61416775321@c.us'` constant added
- `state.lastSeenPMs` tracker added to orbit-state.json
- Orbit restarted successfully after fix (state was local to pollGroups, not global)

### Thomas (SH2) conversation
- Thomas (+33 7 64 08 16 43, `33764081643@c.us`) sent: "Thomas Venant From 36 rosa street send you a message, he leave the room and i wanted to take this room (i share my room with Florent for the moment)"
- Diego couldn't find message via name search — root cause: contact stored as "Unknown" in WA bridge; need phone number to look up directly
- Context: Thomas currently **shares room with Florent** at SH2; wants to move into **Thomas Venant's solo room** (Thomas Venant is leaving/archived)
- Atlas sent French message asking Thomas to clarify: which room, solo or with Florent
- SH2 active as of 7 Mar: Florent (added 18 Jan), Thomas (added 18 Jan), Julia (added 15 Feb, replaced Morgane), Gretel Lay, Sarah = **5 active**
- Thomas Venant: archived, moveOutDate unknown

### WA Bridge contact lookup lesson
- When contact is stored as "Unknown" in bridge, name-based search returns nothing
- Need phone number → JID (`{number}@c.us`) to fetch chat directly
- Keyword search across 638 chats confirmed messages not visible to bridge (sent from native phone app before bridge was linked)

### Resident Registration System — BUILT
- **Purpose**: Index all residents across 15 houses with their name, phone, WhatsApp, nationality, room, move-in date
- **URL format**: `https://housemates.online/r/KEY` (nginx proxy needed) / works now at `https://mc.inspectionsxraytesting.com.au/r/KEY`
- **160 unique keys** generated in `data/registration-keys.json` — per house count = active tenants + 5 buffer
- Each key pre-encodes house code + address (from house-details.json)
- Key counts: BRIS1=10, CO1=11, EB1=11, EB2=15, EB3=10, GS1=12, SB1=11, SH1=10, SH2=10, SH3=11, SP9=10, V5=8, WE1=9, WL3=9, WL4=13
- **Endpoints added to MC server.js**: `GET /r/:key` (serves form), `POST /r/:key` (submits registration)
- **Data files**: `data/registration-keys.json`, `data/resident-registrations.json`
- On submission: key marked used, data saved, Telegram alert sent to Diego
- **Form fields**: Full name, Phone (local SIM), WhatsApp (with "same as phone" checkbox), Email, Nationality, Room description, Move-in date
- "Same as phone" checkbox auto-copies phone → WhatsApp field and disables it
- Form is dark theme, mobile-friendly, shows house address badge (e.g. "📍 36 Rosa St, Spring Hill")

### nginx update PENDING
- Need sudo to update `/etc/nginx/conf.d/housemates.conf` to add `/r/` proxy_pass to localhost:8899
- Command ready for Diego to run manually (requires password)

### Key decisions
- Search by phone number not name when contact is "Unknown" in bridge
- Registration form reviewed at `mc.inspectionsxraytesting.com.au/r/zGFbibIcPa` (SH2 sample)
- Diego will send links one by one per house after form review approved

---

## Session 3 — 7 Mar 2026 (~3:15–3:48pm)

### Registration Form Updates
- **Wise option removed** from payment page (Diego's preference — house bank account only)
- **Documents now served** from MC: `GET /docs/:file` route added to server.js
  - House Rules PDF: `/docs/house-rules-2026.pdf` (919K)
  - Occupancy Licence PDF: `/docs/occupancy-licence-2026.pdf` (275K)
  - Source files copied from `~/.openclaw/media/inbound/` to `public/docs/`
- **Form acceptance section updated**: Two download buttons (House Rules + Occupancy Licence) shown before checkbox; must download before accepting
- **Checkbox wording**: "I have downloaded, read and accept all clauses..."

### Confirmation of Occupancy Letter — BUILT
- **Generator**: `/home/diegopalhano/projects/mission-control/lib/generate_letter.py`
- **Uses Natalie Mosh** exactly as-is (name, email nataliemosh68@outlook.com, phone +61 410 076 937)
- **Signature image** extracted from original DOCX: `lib/natalie-signature.png` — left-aligned above name
- **Uses reportlab** for PDF generation (no npm deps)
- **Auto-fills**: resident name, gender prefix (Mr/Ms), passport (country/number/expiry), move-in date, house address, today's date
- **Letter PDF** auto-generated on form submission → sent via WhatsApp to resident
- Sample letter confirmed good by Diego (signature left-aligned ✅)

### SP9 Viewing Tonight — 6:30pm Surfers Paradise
- **Address**: 4/44 Watson Esplanade, Surfers Paradise (beside Mini Market, that's the entry)
- **Priority order**: Women first, then Japanese, then <35yo, rest excluded
- **Invite draft ready** (6 people, 5-min slots):
  - 6:30 Loll (F26), 6:35 Serena (F27), 6:40 Naiara (F29), 6:45 Nikita (M22), 6:50 Maurice (M26), 6:55 Marco (M28)
- **Excluded**: Matt (no profile), Sergio (male/unknown age), Apo (no profile), Arnold (known from SH1, very messy — permanent block for SP9)
- **Tatsuya** — Japanese guy, new enquirer not yet in database; browser unavailable for scrape
- **Arnold blocked**: Not in DB yet but flagged — do NOT invite to SP9 ever

### Jess Auto-Login Fix
- `#email` selector was timing out — overlays blocking field
- Fix: try multiple selectors (`#email`, `input[type="email"]`, `input[name="email"]`, placeholder fallback)
- Added overlay removal before looking for field (GDPR, cookie, modal classes)
- Login fill now uses dynamic selector detection for both email + password fields
- Jess restarted with fix ~3:44pm

### System Health Incident — 3:40pm
- Monitor bot reported: MC ❌, WA Bridge ❌ (not paired/offline), Jess ✅, Cloudflare ✅
- Fixed: `systemctl --user restart whatsapp-bridge mission-control`
- Both back up — WA reconnected "Strooooong" ✅
- Root pattern: MC + WA bridge dropping together repeatedly — needs investigation

### Arnold (SP9 exclusion)
- Known from SH1 — very messy — **permanently excluded from SP9 viewing invites**
- Not yet in jess-enquirers.json — flag when scrape runs


---

## Session 4 (afternoon, ~4:30–5:10pm)

### Payment Confirmation System (DIN given)
- Sub-agent dispatched to build Telegram inline button + MC Registrations tab + sync logic
- Plan: Hakim submits form → Telegram alert with [✅ Confirm Payment] / [❌ Reject] inline keyboard
- On confirm (either Telegram OR MC button): `paymentConfirmed: true` in resident-registrations.json + added to tenants.json as active occupant
- If confirmed via one, the other auto-reflects (disabled/greyed)
- Single source of truth: `data/resident-registrations.json`
- Sub-agent runId: `0492664f-e23d-421c-8759-d4d997fa38b0`

### Test Registration Key for Diego
- Key: `2XOZQlq3uy` (BRIS1 house) — test key sent to Diego's WA (+61416775321)
- URL: `https://form.housemates.online/r/2XOZQlq3uy`
- To be deleted after Diego confirms flow works

### SP9 Inspection Tonight — Emilio assigned
- SP9 inspection tonight; Emilio is the assistant (not Dee)
- Emilio's number: +61481517377
- All 4 original queue entries (Loll, Serena, Nikita, Maurice) updated to Emilio
- Naiara (201143853) and Marco (201145207) convIds found in jess-enquirers.json and added to queue
- Additional people added: Matt (201144022), Sergio (201147519), Isha (201145974), Apo (201160116), Catalina (201143842)
- **Total queued: 11 people** — none sent yet (Jess session broken)

### Jess Session / Cookie Fix
- Flatmates cookies expired (last saved Mar 2) — root cause of blank pages
- Extracted fresh cookies from Chrome using `browser_cookie3` library (pip installed)
- Chrome on this system uses v11 encryption with GNOME keyring (secretstorage); 64-byte raw secret
- Saved to both `flatmates-cookies.json` AND `session.json` (16 cookies including `_flatmates_session`)
- Jess log doubling bug fixed: service was writing to stdout AND Jess wrote to file directly → `StandardOutput=null` / `StandardError=null` in jess.service
- Single instance confirmed (PID 184742), no more duplicate log lines
- **BUT: Flatmates still serving blank pages to Playwright** — title="" allLinks=0 — not a cookie issue, it's fingerprinting/bot detection at a deeper level
- Flatmates API tested: GraphQL (/graphql) → 404, REST endpoints → 404, no public API available

### Chrome Cookie Extraction Method (for future)
- Command: `DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$(id -u)/bus" python3 -c "import browser_cookie3, json; cj = browser_cookie3.chrome(domain_name='.flatmates.com.au'); ..."`
- Library: `browser_cookie3` (pip3 install browser_cookie3)
- Chrome v11 cookies on this machine use GNOME keyring via secretstorage
- This gets the decrypted cookie values successfully

### System Health (5pm)
- MC: ❌ down (restarted → active ✅)
- WA Bridge: ❌ down (restarted → active ✅, ready: true)
- Jess: ❌ down (restarted → active ✅, but Flatmates session still blocked)
- Crash pattern: stale Chromium from Jess eating CPU → MC + WA bridge starve
- Root fix needed: Jess must not accumulate Chromium processes

### SP9 Manual Send Required
- Browser relay (Chrome extension) unreliable — tab not found errors repeatedly
- API approach failed (all 404)
- Diego must send all 11 SP9 invites manually from flatmates.com.au/messages
- Message template: "Hey [Name]! We have a room viewing tonight at SP9 — 4/44 Watson Esplanade, Surfers Paradise (entry beside the Mini Market on Watson Esplanade). Emilio will be there to show you around 😊 +61481517377 (WhatsApp)"

### Jess Stealth Mode Status
- headless: false + anti-detection flags working for single instance
- But Playwright fingerprint is still detected by Flatmates — browser viewport/canvas/WebGL fingerprinting likely
- Next step: investigate Playwright stealth plugins (playwright-extra + puppeteer-extra-plugin-stealth) or use real Chrome binary instead of Chromium

---

## Session 5 (evening — 8pm)

### Jess Proxy Fix — COMPLETED ✅
- Root cause of Flatmates block: **datacenter IP** at network level (not fingerprinting)
- Fix: SSH SOCKS5 tunnel through desktop-1 (home IP: 118.208.196.152, residential AU)
- Diego added server SSH key to `~/.ssh/authorized_keys` on desktop-1 (done manually)
- Confirmed SSH tunnel works: `ssh -D 1080 -N diegopalhano@100.92.117.73`
- `socks-tunnel.service` created and enabled: `~/.config/systemd/user/socks-tunnel.service`
- SOCKS5 proxy verified: curl via `socks5h://127.0.0.1:1080` → home IP confirmed

### Jess Chrome Crash Root Cause & Fix
- Crash: `Target page, context or browser has been closed` on every navigation
- Root cause: Playwright's `proxy: { server: '...' }` option injects `--host-resolver-rules="MAP * ~NOTFOUND , EXCLUDE 127.0.0.1"` which conflicts with Chrome's internal CDP routing when using SOCKS5
- Fix: removed Playwright `proxy` option; added `--proxy-server=socks5://127.0.0.1:1080` and `--proxy-bypass-list=127.0.0.1;localhost;::1` as raw Chrome args
- Also removed `ExecStartPre` from jess.service (was causing "fatal signal to control process" by killing Chrome during legitimate startup)
- Jess now stable: browser launches, navigates without crashing, scrapes CO1/EB2/SP9/BRIS1 ✅
- Flatmates returning 429 (rate limited from today's volume) — will clear overnight
- Jess service status: **active** as of 8:27pm; no browser crashes

### Watchdog Chrome Bomb Guard Fixed
- Was killing Chrome with threshold >4 processes — too low, killed Jess during startup
- Updated threshold to >12 AND only when Jess is NOT active
- Pattern: `pgrep -f "google/chrome/chrome" | xargs -r kill -9` (not broad pkill)

### jess.service Updated (final state)
- Removed `ExecStartPre` entirely
- `Restart=no` (watchdog manages restarts)
- `StandardOutput=null` / `StandardError=null`

### Desktop Sharing Enabled
- Diego enabled desktop sharing security option on Rocky Linux
- NoMachine (port 4000) + Termius: BACK ONLINE ✅
- Full remote access stack confirmed: Tailscale + NoMachine + Termius + SOCKS5 proxy
- Pre-bali checklist: remote access item marked ✅

### Pre-Departure Status (13 days out)
- Tech stack: fully sorted (remote access, monitoring, proxy, Jess)
- Jess will be functional for messages once Flatmates rate limit clears (tomorrow morning)
- SP9 queue (11 messages) still needs to be sent once Jess inbox is accessible
