## Auto Task Extraction — Review Log
Started: 2026-02-28
Method: Extract task-like messages from WhatsApp work groups → populate mc.tasks
Status: IN PROGRESS
Review point: Check dashboard Tasks tab after extraction, fix mis-categorised items manually

### What to review:
- Messages that look like tasks but are actually conversations
- Tasks assigned to wrong house
- Duplicate tasks
- Tasks already completed that show as pending

### Fix path:
- Dashboard Tasks tab → each task has Edit/Delete
- Can re-run extraction after bridge has more data
- Script: ~/projects/whatsapp-bridge/extract-tasks.js

## UX Rule — Commands
Always send shell commands one per message, never chained in a block.
Makes it easy to copy on mobile without selecting specific lines.

## Project Idea: Manager Mobile App
Status: FUTURE — to be vibe-coded later
Priority: Medium

### Core concept
Native iOS + Android app for Mathis, Emilio (and future managers).
Login → their panel only (no owner data visible).

### Features from Timeroo-style workflow
- GPS location tracking — log arrival/departure at assigned house
- Clock in/out at house (geofenced to property address)
- Pre-set task descriptions (dropdown: cleaning, inspection, maintenance, tenant issue, etc.)
- Photo reports — attach photos to tasks on-site
- Inspection reports — room-by-room checklist with photos
- Payslips — view their own payslips in-app

### Atlas suggestions to add
- Push notifications for new tasks assigned to them
- Bin day reminders (their house only)
- Tenant move-in/move-out checklists
- Maintenance job timer (start/stop per task)
- Chat with owner (in-app, not WhatsApp)
- Bond collection confirmation with photo of receipt
- Weekly summary auto-submitted to owner every Sunday

### Tech direction
- React Native (single codebase iOS + Android)
- Auth: same server-side manager auth as Mission Control
- API: extends existing mission-control server.js endpoints
- Offline-first: cache tasks locally, sync when connected

### Notes
- Manager panels already exist in Mission Control web — app mirrors that data
- Vibe-code session needed to scaffold the app
- Owner to define payslip format before building payslip feature
