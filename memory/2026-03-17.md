---
hyperspell_id: D_19Q-vdASKviw
---
# Memory Flush — 17 Mar 2026 (Evening)

## Network & Remote Access — RESOLVED (permanent)

### Firewall fix (recurring issue — now locked)
- Root cause: `/etc/firewalld/zones/` directory didn't exist → firewalld reset to defaults on every restart
- Fix applied: `sudo firewall-cmd --zone=trusted --add-interface=enp131s0 --permanent` + `--add-interface=enp128s20f0u3u4`
- `firewalld` now enabled (`systemctl enable firewalld`) → starts on boot with zones intact
- Both ethernet + tethering interfaces in trusted zone permanently
- This issue has happened 3-4 times total — should not recur

### Ethernet cable vs tethering priority
- Cable `enp131s0` = `192.168.1.x`, gateway `192.168.1.1`, metric set to **10**
- Tethering `enp128s20f0u3u4` = `10.80.x.x`, metric set to **500**
- Cable is now always primary; tethering is automatic fallback
- Config saved permanently in NetworkManager connection profiles

### RustDesk permanent auto-start
- `sudo systemctl enable rustdesk` → enabled (system service)
- `sudo loginctl enable-linger diegopalhano` → Linger=yes
- RustDesk starts on boot before user login
- Peer config: `~/.config/rustdesk/peers/427728789.toml`, keyboard_mode changed to `legacy`

### RustDesk mouse cursor mismatch
- Confirmed Wayland bug — cursor offset due to display scale factor not applied
- Fix: PR #13498 merged in 1.4.x; requires **"Relative mouse mode"** on client (laptop)
- In RustDesk toolbar while connected → mouse icon → Relative mouse mode
- RustDesk keyboard input on Android = known open bug (Android → Wayland, map mode only, no fix released)

### Server reboot recovery
- Auto-login: `AutomaticLoginEnable=True`, `AutomaticLogin=diegopalhano` confirmed in `/etc/gdm/custom.conf`
- Wait ~90 seconds after reboot before reconnecting
- All services (MC, WA bridge, RustDesk) auto-restart on boot

## MC Fixes (earlier in session)

### Bond tracker reconciliation (commit `302cb2f`)
- "Mark Paid & Archive" button fixed by Smith (subagent `68479627-8a0c-4e10-b5e2-481df8761c6c`)
- UI was showing `bond-tracker.json` (old) but PATCH was hitting `bond-return-requests.json` (new)
- Fix: server now handles both old and new system entries in same PATCH endpoint

## Key Identifiers
- Ethernet interface: `enp131s0`
- Tethering interface: `enp128s20f0u3u4`
- Router IP: `192.168.1.1`
- RustDesk peer config: `~/.config/rustdesk/peers/427728789.toml`
- RustDesk system service: `/etc/systemd/system/rustdesk.service`

## Rent Pricing Rule (locked 18 Mar 2026)
- Rooms are rented as a **whole room**, never as beds or split prices
- Weekly rent = the room price, regardless of how many people share it
- Couples/sharers split the cost privately between themselves
- Registration forms always show the **full room rent** (e.g. $500/wk for R2 BRIS1, not $250/person)
- Never create reg keys with per-person split amounts

## Addendum (18 Mar 2026)
- Sharers CAN pay their half separately (we accept it) but we never promote or suggest it
- Always quote/register at full room price — how they pay is their business
