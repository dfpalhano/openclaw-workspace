## 2026-03-28 — Durable notes (append)
- Mission Control staff/manager surface updated for Lenny in `/home/diegopalhano/projects/mission-control/data/managers.json` with WA `33783146859@c.us`, role `manager`, active `true`, houses = all houses, languages `FR/EN`. Backup created: `managers.json.bak-20260328-141742`.
- Mission Control assistant roster code path currently lives in `/home/diegopalhano/projects/mission-control/server.js` and was expanded to accept `lenny` alongside `dee` and `mathis`:
  - allowed assistant set changed from `['mathis','dee']` to `['mathis','dee','lenny']`
  - ASSISTANTS map now includes `lenny: { name: 'Lenny', phone: '+33 7 83 14 68 59' }`
- Attempted `systemctl restart mission-control.service` after Lenny assistant code change timed out because service restart loop hit `EADDRINUSE` on `0.0.0.0:8899`; code is saved but live activation not confirmed from that restart path.
- Lead Count reconciliation bug root cause: displayed `Total` came from historical `jess-thread-states.json` lifecycle totals while visible buckets (New/Unanswered/Answered/Dead) came from the 3-day relay window. This caused totals like SH1 85 vs visible bucket sum 50.
- Smith fixed Lead Count in `/home/diegopalhano/projects/jess-bot/jess-v3.js` so displayed `Total = New + Unanswered + Answered + Dead`, all from the same 3-day relay dataset. Added per-house debug logging for bucket thread IDs, total thread IDs, and duplicates. Commit: `ee4f11d`.
- WA confirmation rule violation repeated on 2026-03-28: after Diego said `Send her` for Becca, Atlas incorrectly asked for another confirmation instead of sending immediately. Locked behavioural rule restated: for individual WA sends, once the exact draft is shown and Diego gives a clear execution instruction (`send her`, `send it`, `yes send`, `confirm`, etc.), send immediately with no extra confirmation layer.
- Becca inspection/occupancy clarification message was ultimately sent successfully via WA bridge to `447480213859@c.us`; bridge result `{"ok":true}`.
- Live WhatsApp verification for Lenny (`33783146859@c.us`) across all 16 house groups showed he was only present in SH1 and missing from all others. This confirms prior bulk add attempts did not actually land across all groups and the WA bridge group-add path still needs per-group verified retries/debugging.

- Locked payment-clarity rule for occupancy/payment sends: offer messages must make payment timing explicit. Required lines/meaning: amount due now, what the amount includes, whether first week is due now or on move-in, `Please send the payment receipt once transferred`, and `Receipt confirms the booking and commencement date`. Operational split locked: offer card alone is not enough when payment timing is ambiguous; use occupancy summary/additional information style for payment clarity.
- Mission Control template updated at `/home/diegopalhano/projects/mission-control/data/occupancy-offer-template.json` to preserve explicit payment clarity wording including `First week: may be paid upon move-in`, `Total due now: ${{bond}}`, `Please send the payment receipt once transferred`, and `Receipt confirms the booking and commencement date`. Backup: `occupancy-offer-template.json.bak-20260328-161454`.
