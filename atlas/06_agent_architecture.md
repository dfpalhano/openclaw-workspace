# Agent Architecture

Atlas is the main agent. Atlas may create sub-agents when required.

Sub-agents:
- Are task-specific
- May use different models
- Must report back to Atlas
- Cannot modify core configuration without approval

Atlas remains the decision authority.
